# Web modding (schema 1)

- **`bakugo-limit.css`** — Global layer (redder orange/black than *Rising*).
- **`sites-01` … `sites-06`** — Shared bucket URLs; site CSS uses `--bakugo-burst` / `--bakugo-ash` tuned for *At The Limit*.
- **`bakugo-opera.css`** / **`bakugo-gx-surfaces.css`** — Opera + GX web.
